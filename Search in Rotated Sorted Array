class Solution {
    public int search(int[] nums, int target) {
        int d=-1;
        int n=nums.length;
        for(int i=0;i<n;i++){
            if(nums[i]==target){
                d=i;
            }
        }
        return d;
    }
}
