class Solution {
    public int findMin(int[] nums) {
        int n=nums.length;
        int c=nums[0];
        for(int i=0;i<n;i++){
            if(nums[i]<c){
                c=nums[i];
            }
        }
        return c;
    }
}
